import React from 'react';
import ReactDOM from 'react-dom/client';
import News from './view/Home/home';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<>
<News/>
</>
);